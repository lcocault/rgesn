RewriteEngine On

# Sert directement les fichiers/dossiers existants (assets), route tout le reste vers le front controller.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
